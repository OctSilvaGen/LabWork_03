#include <LedControl.h>


LedControl lc = LedControl(11, 13, 10, 1);

void setup() {
  lc.shutdown(0, false);      
  lc.setIntensity(0, 10);       
  lc.clearDisplay(0);          
  
  for (int i=0; i<8; i++) 
    lc.setLed(0, i, i, true);
}

void loop() { 
  
}
